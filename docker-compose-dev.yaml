volumes:
  dbdata:

services:
  db:
    image: mongo:8.0
    environment:
      MONGO_INITDB_ROOT_USERNAME: bctool
      MONGO_INITDB_ROOT_PASSWORD: phev
    volumes:
      - type: volume
        source: dbdata
        target: /data/db
    ports:
      - "127.0.0.1:27017:27017"
